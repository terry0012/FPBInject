############################################################################
# FPBInject/Make.defs
#
# SPDX-License-Identifier: MIT
#
############################################################################

ifneq ($(CONFIG_FPBINJECT),)
CONFIGURED_APPS += $(patsubst $(APPDIR)/%,%,$(patsubst %/,%,$(dir $(lastword $(MAKEFILE_LIST)))))
endif
